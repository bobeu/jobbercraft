// export const blockie = ({account, size} : {account: string, size?: number}) => {
//   return `${account.substring(0, size || 4)}...${account.substring(account.length - (size || 4), account.length)}`
// }
